<% @title = @cocktail.name %>
<% @left_nav_icon = (link_to icon('fas', 'cocktail'), cocktails_path, class: "btn btn-primary") %>

<div class="bg-image p-5" style="
    background-image: url(<%= @cocktail.img_url %>)
    ">
<%= render 'components/navbar' %>
</div>

<div class="d-flex justify-content-between align-items-center">
    <h4>Ingredients</h4>
    <%= link_to "Autofill details", cocktail_path(@cocktail), method: 'patch' %>
</div>
<%= render 'components/cocktail_doses_list' %>
<br>
<%= render "components/add_ingredient_form" %>